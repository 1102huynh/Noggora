ANTHROPIC_API_KEY=            # optional — nếu trống, dùng manual script mode
PEXELS_API_KEY=                # optional — nguồn B-roll #1; nếu trống, thử Pixabay rồi mới đến data/assets_local/
PIXABAY_API_KEY=               # optional — nguồn B-roll #2, dùng khi Pexels trống/không đủ kết quả
ELEVENLABS_API_KEY=            # optional — chỉ dùng nếu VOICE_PROVIDER=elevenlabs
